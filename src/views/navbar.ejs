<nav class="navbar navbar-expand-lg navbar-dark bg-info">
  <div class="container">
    <div class="navbar-brand d-none d-md-block">
      <img src="https://upload.wikimedia.org/wikipedia/commons/4/4f/Twitter-logo.svg" alt="logo" width="30" />
      MyTwitter
    </div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active <%= values.isLogedInUser ? '' : 'd-none' %>" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item <%= values.isLogedInUser ? '' : 'd-none' %>">
          <a class="nav-link" href="/twitters">Twitters</a>
        </li>
        <li class="nav-item <%= !values.isLogedInUser ? '' : 'd-none' %>">
          <a class="nav-link" href="/login">Login</a>
        </li>
        <li class="nav-item dropdown <%= values.isLogedInUser ? '' : 'd-none' %>">
          <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown"
            aria-expanded="false">User</a>
          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
            <li>
              <a class="dropdown-item <%= values.isLogedInUser ? '' : 'd-none' %>" href="/logout">Logout</a>
            </li>
            <li>
              <a class="dropdown-item dropdown-item <%= values.isLogedInUser ? '' : 'd-none' %>" href="#">Your custom
                action</a>
            </li>
            <li>
              <hr class="dropdown-divider" />
            </li>
            <li>
              <a class="dropdown-item <%= values.isLogedInUser ? '' : 'd-none' %>" href="/logout">Logout</a>
            </li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>